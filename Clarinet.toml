[project]
name = 'Gift-Circle'
description = ''
authors = []
telemetry = true
cache_dir = '.\.cache'
requirements = []
[contracts.gift_circle]
path = 'contracts/gift_circle.clar'
clarity_version = 2
epoch = 'latest'
[repl.analysis]
passes = [
    'call_checker',
    'check_checker',
]

[repl.analysis.lint_groups]

[repl.analysis.lints]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false

[repl.remote_data]
enabled = false
api_url = 'https://api.hiro.so'
